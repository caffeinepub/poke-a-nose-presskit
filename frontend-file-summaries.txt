{
  "frontend/index.html": "HTML entry point with Google Fonts preconnect and Fredoka One + Inter font imports.",
  "frontend/src/index.css": "Global styles with OKLCH black-and-white design system, page background image handling with dark mode inversion, game/festival logo inversion classes, body text color CSS variable, screenshot borders, and smooth theme transitions.",
  "frontend/tailwind.config.js": "Tailwind config extended with Fredoka One heading font, Inter body font, compact border radius, and all OKLCH color tokens.",
  "frontend/src/contexts/ThemeContext.tsx": "React context providing dark/light theme state with localStorage persistence, toggle function, and automatic HTML class management.",
  "frontend/src/hooks/useQueries.ts": "All React Query hooks for backend integration including useGetContent, useIsAdmin, useGetPasswordProtectionStatus, all individual field queries, and all CMS mutation hooks with proper actor availability checks, retry logic, and cache invalidation.",
  "frontend/src/App.tsx": "Main app with TanStack Router setup for three routes (/, /press-kit, /admin), QueryClientProvider with retry/staleTime defaults, custom ThemeProvider, and a BodyTextColorApplier that reads body text color from backend content using the renamed `useGetContent` hook.",
  "frontend/src/components/Header.tsx": "Minimal header with only the Press Kit navigation link and dark/light mode toggle; the \"Poke A Nose\" title text has been removed and the layout is right-aligned. Hidden on the /admin route.",
  "frontend/src/components/NewsletterPlaceholder.tsx": "Modular non-functional newsletter signup component with email input and subscribe button.",
  "frontend/src/components/VideoSection.tsx": "Video section component that uses `/assets/video_thumbnail.png` as the static thumbnail, shows a play button overlay when a YouTube link is available, and switches to an embedded iframe on click.",
  "frontend/src/components/FullscreenViewer.tsx": "Fullscreen image viewer overlay — fixed duplicate `onClick` attribute by keeping only one handler on the `<img>` element and using `stopPropagation` on the close button.",
  "frontend/src/components/ScreenshotsGallery.tsx": "Responsive 2–3 column screenshot grid using hardcoded static asset paths (screen01–screen06), with aspect-video thumbnails, hover scale effect, and fullscreen viewer on click.",
  "frontend/src/components/MediaDownloadButton.tsx": "Download button that triggers direct ZIP file download with correct MIME type.",
  "frontend/src/components/SocialMediaSection.tsx": "Conditionally rendered Instagram link (only when a valid URL is provided), showing the Instagram icon image and a labeled link that opens in a new tab.",
  "frontend/src/components/PasswordGateModal.tsx": "Password gate modal rendered as a full-screen fixed overlay with z-index 9999; uses the actor directly to call `verifyPassword`, stores verification in sessionStorage on success, shows inline error messages, and handles actor loading state gracefully.",
  "frontend/src/components/ImageWithFallback.tsx": "Reusable image component with loading skeleton and error fallback states.",
  "frontend/src/pages/LandingPage.tsx": "Landing page showing a static enlarged game logo and a newsletter signup iframe centered below it; the duplicate local `<Header />` import and render have been removed since the header is already provided by the root layout in App.tsx.",
  "frontend/src/pages/PressKitPage.tsx": "Full press kit page that reads all text content from `useGetContent` and password status from `useGetPasswordProtectionStatus`; fixes the `instagramUrl` prop name (was incorrectly `instagramLink`) passed to `SocialMediaSection`.",
  "frontend/src/pages/AdminDashboard.tsx": "Full CMS admin dashboard with self-contained login UI; `login()` is called without arguments (fixing the TS2554 error), with sequential loading states, inline save buttons with spinners per section, and logout button.",
  "frontend/src/hooks/useActor.ts": "Initializes the backend actor — creates an authenticated actor using HttpAgent.create with the II identity (passing only `agent` to createActorWithConfig) or an anonymous actor (passing only `agentOptions`), with proper shouldFetchRootKey handling for local vs production environments. No longer calls any non-existent initialization methods that would cause actor creation to fail."
}